apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: deploy-ingress

spec:
  ingressClassName: nginx

  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: deploy-service
                port:
                  number: 80
